# Data manifest: <file> <sha256>, one per line. Used by tools/fetch-data.sh.
#
# Provenance and limits: the provider's .md5sum sidecars are gone from the
# archive (the index lists them; fetching returns 404), so a provider-published
# digest cannot be corroborated as of this entry. Each digest below is the
# sha256 of two independent TLS fetches from the provider's own host that
# agreed byte for byte, plus a clean gzip CRC pass over the full stream. That
# pins exactly which bytes the archive serves and detects transfer corruption;
# it cannot detect the archive itself serving altered data. If a provider
# digest reappears, corroborate these values against it retroactively.
01302019.NASDAQ_ITCH50.gz 8c97b5b13bc451c012c2466fb7e258da134dab29aa47b67fe7b0088c78e870be
